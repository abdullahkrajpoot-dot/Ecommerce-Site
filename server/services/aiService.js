// AI integration service (stub)
// Add connectors for OpenAI / LLM orchestration, embeddings, and vector DBs here.

exports.generateProductDescription = async (product) => {
  // TODO: call LLM with product data and return generated copy
  return `Smart ${product.name} — concise product description.`;
};
