# Frontend Layer

This folder represents the user-facing presentation layer.

Current responsibilities:
- Product catalog UI
- Homepage and shopping pages
- Contact and review sections
- Order form interaction and client-side flow

Main entry:
- index.html

The frontend communicates with the backend via:
- /api/create-order

This separation keeps the UI independent from data storage and server logic.
